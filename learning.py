import pyautogui

while True:
    a = pyautogui.position()
    print(a)
    # Write a pyautogui script which which will first click on an icon at  1400, 1038                                               
    # 690 224 to 1859 954 to select a text and then copy it to the clipboard then take it in a variable